name: se_stock_app
description: Inventeringsapp med realtime Firebase för flera lager
publish_to: 'none'
version: 1.1.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.1
  flutter_riverpod: ^2.6.1
  csv: ^6.0.0
  share_plus: ^10.0.0
  path_provider: ^2.1.4
  intl: ^0.19.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true